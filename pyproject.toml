[project]
name = "enrolbound"
version = "0.1.0"
description = "ENROLBOUND — Gateway rule changes the enrolment answer without changing agent code"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.10",
    "pydantic-ai>=1.16",
    "pydantic-settings>=2.6",
    "logfire[fastapi]>=3.0",
    "python-dotenv>=1.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
prometheux = ["prometheux-chain>=0.1"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
enrolbound = ["static/*"]
